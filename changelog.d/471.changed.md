- **Backfilled pages go through the normal extraction gates instead of always
  staging.** The origin stamp routed every reconstructed page to
  `shared/_inbox/`. Nobody drained the inbox, so a new user's history made
  nothing live, and the reflex fired on 0 of the next 50 prompts. Backfilled
  rules now take the evidence gate, the reference gate and the project path
  like any other, and keep `origin: backfill` as provenance. The bar was
  declared before labelling (≥ 85% good under two blind raters) and held:
  40/47 = 85.1% of what those gates let through
  (`tools/measure_backfill_routes.py`). A page an older mnemo already staged
  stays staged, and a backfilled page still never promotes universally.
  (#471)
