"""Autopilot — autonomous monitoring + self-fix loop for mnemo.

This package hosts the foundational core (proposals, hook-driven scheduler,
kill switch, PR budget, frozen recall set, label constants) plus 4 Tier
sub-packages (insights, selffix, tuner, proposer) that are populated by
separate Tier specs and merged sequentially.
"""
